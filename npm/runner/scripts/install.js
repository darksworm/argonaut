/* eslint-disable no-console */
console.error('[argonaut] Using npm wrapper that launches the native binary.');
console.error('If the binary fails to run, ensure the correct platform package is present');
console.error('or install via brew/scoop/winget/curl: https://github.com/darksworm/argonaut#install');

