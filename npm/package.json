{
  "name": "argonaut-cli",
  "version": "2.0.5",
  "description": "Argonaut CLI (Go binary via per-platform npm packages)",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "postinstall": "node scripts/postinstall.js"
  }
}

